name: eyrie

services:
  eyrie:
    build:
      context: ../../
      dockerfile: Dockerfile
    image: ghcr.io/graycodeai/eyrie:dev
    ports:
      - "8080:8080"
    env_file:
      - path: ../../.env.example
        required: false
    volumes:
      - eyrie-data:/data

volumes:
  eyrie-data:
